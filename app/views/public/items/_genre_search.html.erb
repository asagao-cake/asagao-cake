<!--ジャンル検索の部分テンプレート-->

<table class="table table-borderd">
  <thead>
    <tr>
      <th>ジャンル検索</th>
    </tr>
  </thead>
  <tbody>
    <% genres.each do |genre| %>
      <tr>
        <td><%= link_to genre.name, items_path(name: genre.name) %></td>
      </tr>
    <% end %>
  </tbody>
</table>